export interface Dot {
    radius: number;
}

export interface Circle {
    radius: number;
    width: number;
}

export interface Branch {
    length: number;
    width: number;
    angle: number;
    mirror: boolean;
}
